<%= form_for(@memer) do |f| %>
  <% if @memer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@memer.errors.count, "error") %> prohibited this memer from being saved:</h2>

      <ul>
      <% @memer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :gender %><br>
    <%= f.text_field :gender %>
  </div>
  <div class="field">
    <%= f.label :memeCount %><br>
    <%= f.number_field :memeCount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
